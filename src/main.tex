\documentclass[12pt]{article}
\usepackage[margin=1in]{geometry}
\usepackage{amsmath, amssymb, amsthm}
\usepackage{graphicx} % Required for inserting images

\newtheorem{theorem}{Theorem}
\newtheorem{definition}{Definition}
\newtheorem{lemma}{Lemma}
\newtheorem{correlate}{Correlate}

\setlength{\parindent}{0pt}

\title{Essay on an Ontology with mathematical Clarity}
\author{Creddilles Branaris}

\begin{document}

\maketitle

List of titles
- Seinsforschung mit mathematischer Klarheit
- Versuch einer mathematischen Reduktion des in der Welt seins
- Das Sein in mathematischer Klarheit porträtiert

\section{Deutsch}





\section{English}

\subsection{Introduction}
The question, whether being is influenced by language is a tricky one. 

One might call this attitude reconstructivism.

\subsection{The problem between epoche and fragmentation of thought}

- Epoche is a kind of desintegration.

\subsection{Some section}



\subsection{Section 1}

Every factor $\textbf{f}_{\text{XY}}$ contains a set $S_{\text{XY}}$ and a function $f_{\text{XY}}$ on the set of all compostites $S_{31}$. 

\subsection{Section 2}

A composite is a set of entities:
\( c_t = \{ e_1, \dots, e_n \}.\)
The set of all composites is $S_{31}$. An entity $e_t$ is either a composite or a particle. It has position $\text{f}_{15}(e_t)$, velocity $\text{f}_{16}(e_t)$ and acceleration $\text{f}_{17}(e_t)$. The integrity of a composite is $e_t$ to the power of the delta of its volume:
\(
\text{f}_{14}(c) = e^{\Delta v(c_t)}.
\)
The volume of a composite is the Lebesgue measure of its convex hull:
\[
v(c) = \lambda(\operatorname{conv}(c_t)).
\]

The convex hull of a composite is called its body:
\[
\text{f}_{02}(c) = \operatorname{conv}(c_t).
\]

The centeredness of a composite $s$ is defined as the integral of its volume:

\bigskip

\textbf{State-based definitions:}

$f_{93}(t)$ is the set of all particles that are in the  
\[
f_{93}(t) = \{c\in \text{f}_{31} |i(c(t)) < 1\} \quad \text{(collapse)}
\]

\[
f_{13}(c) = \{\, s \mid i(c,s) > 0 \,\} \quad \text{(fragmentation)}
\]

"The set of states of $c$ where $i(c) > 1$"\\

A composite with an integrity of 1 behaves like a particle:
\[
i(c) = 1.
\]

\[
\text{f}_{02}(c) = \{\, s \mid i(c,s) = 0 \,\} \quad \text{(body as equilibrium states)}
\]

\subsection{Example}

The most radical form of authenticity is to act with integrity regarding the true self even if it means death.

\[
\text{action} := a \in 48
\]
\[
\text{self} := s \in 35\\
\]
\[
r := 31(a, s) 
\]

\end{document}
